# Determines whether a path is an HTTP(S) URL.

Determines whether a path is an HTTP(S) URL.

## Usage

``` r
.is_url(path)
```

## Arguments

- path:

  A character string.

## Value

Logical scalar indicating if it's a valid URL.

# Given a \*.csv(.gz) path, return \*.parquet path in a /subset folder

Given a \*.csv(.gz) path, return \*.parquet path in a /subset folder

## Usage

``` r
.csv2parquet_path(csv_path)
```

## Arguments

- csv_path:

  Path to the csv file.

## Value

A character string.

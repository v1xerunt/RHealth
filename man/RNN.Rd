% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Model_RNNModel.R
\name{RNN}
\alias{RNN}
\title{RNN Model Class}
\usage{
RNN(dataset = NULL, embedding_dim = 128, hidden_dim = 128, ...)
}
\arguments{
\item{dataset}{A \code{SampleDataset} object providing input/output schema.}

\item{embedding_dim}{Integer, embedding width for each token.  Default 128.}

\item{hidden_dim}{Integer, hidden size of each RNN.  Default 128.}

\item{...}{Additional arguments forwarded to each \code{RNNLayer} (except
\code{input_size}/\code{hidden_size}, which are fixed by \code{embedding_dim} /
\code{hidden_dim}).}
}
\description{
A full‑featured classification model that
(i) embeds each input feature with \code{EmbeddingModel},
(ii) processes each feature sequence through its own \code{RNNLayer}, and
(iii) concatenates the final hidden states for prediction.
}
\details{
\itemize{
\item Works for binary, multi‑class, or regression labels (inferred from \code{dataset}).
\item Supports optional \code{mask} per feature (all‑zero rows are treated as padding).
\item All internal indices comply with R's 1‑based rule; no device mismatches.
}
}
